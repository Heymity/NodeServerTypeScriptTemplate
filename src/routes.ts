import express from 'express';

//import MyController from './Controllers/MyController';

const routes = express();

//const myController = new MyController();

//routes.get('/items', myController.index);

export default routes;
